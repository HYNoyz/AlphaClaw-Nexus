version: '3.8'

services:
  alphaclaw-core:
    build: .
    container_name: alphaclaw-nexus-engine
    ports:
      - "5000:5000"
    restart: unless-stopped
    # 如果你的 TG Bot 需要读取本地的 .env 里的私钥或 API Key，加上这行：
    env_file:
      - .env
